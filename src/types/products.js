// @flow
export type ProductType = {
  title: string,
  subtitle: string,
  image: string,
  id: string,
  price: number,
  subtotal: number,
  description: string
};

export type ProductsArrayType = ProductType[];
